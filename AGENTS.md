# 控谱项目开发规则

## 文件安全

- 禁止批量删除文件或目录。
- 不得使用 `del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse` 或 `rm -rf`。
- 需要删除文件时，只能一次删除一个经过核对的明确文件路径。
- 如果确实需要批量删除，必须停止操作并请用户手动处理。
- 不得回退或覆盖用户已有改动；遇到相关改动时应在现有状态上继续开发。

## 当前资源条件

- 当前用户是机械工程师，不具备代码或电气控制专业背景，不能承担日常代码审查、电气逻辑审查或调试决策。
- 当前没有可用 PLC 硬件，也暂时没有电气工程师持续参与。
- 不得把常规技术判断、代码选择或可自动完成的验证转交给用户。Agent 应自行调研、实现、测试、审计并记录依据。
- 只有当工作确实依赖 PLC 硬件、厂商软件、现场设备、安全回路资料或电气工程师专业判断时，才登记为外部验证项。

## 自主开发原则

- 在不需要 PLC 硬件或电气工程师现场参与的范围内，持续开发到功能、测试、文档和审计闭环，不因缺少人工确认而提前停止。
- 优先使用确定性实现、自动化测试、静态分析、仿真夹具、黄金样例、属性测试和回归测试验证功能。
- 对陌生能力先调研官方文档、厂商公开资料、标准、成熟开源项目、可复现样例和许可证，再决定实现方式；不得凭假设宣称可行。
- 调研开源项目时应记录来源、许可证、维护状态、适用范围、已知限制以及是否实际运行验证。不得直接复制许可证不兼容或来源不明的代码。
- 对生成器、解析器、规则引擎、版本模型和安全门禁进行独立审计，覆盖正常路径、失败路径、边界输入、并发冲突、路径穿越、工件不可变性和结果可复现性。
- 用户不能进行代码层人工干预，因此 Agent 应自行修复测试失败、依赖冲突、迁移问题和可诊断的运行错误；只有缺少必要外部信息或权限时才请求用户处理。
- 可以选择保守默认值继续开发，但必须把假设、风险和后续验证方法写入文档。

## 验证等级与表述

所有功能必须使用下列验证等级之一，不得混淆：

1. `代码完成`：实现和代码审查已完成，但尚未运行自动化验证。
2. `自动验证通过`：单元、集成、E2E、静态检查或确定性对照测试已通过。
3. `厂商工具验证通过`：已由真实 GX Works3、GX Simulator3 或其他目标厂商工具完成导入、编译或模拟验证。
4. `硬件实测通过`：已在明确型号的真实 PLC、I/O 和现场设备上按测试规程通过。
5. `电气工程师确认`：已由具备相应能力的电气工程师审核控制逻辑、互锁、安全边界和异常策略。

- 未达到某一级时必须明确写“未验证”或“待验证”，不得使用“可用”“通过”“已连接”“安全”等容易被理解为更高验证等级的表述。
- 软件模拟不能替代硬件实测；普通 PLC 逻辑验证不能替代安全 PLC、急停、门锁、强制输出或机械风险评估。
- M1/M2 生成的 FX5U Structured Text 是确定性工程骨架。在厂商工具和硬件验证前，禁止宣称可直接下载到 PLC 或用于生产。

## 集中验证包

- 将所有必须依赖电气工程师、厂商软件或 PLC 硬件的事项持续登记，不要零散打断用户。
- 在可自动开发的功能基本完成后，整理一个可在 1–2 天集中执行的验证包。
- 验证包至少包含：环境与软件版本、PLC/模块/接线清单、测试前置条件、逐项步骤、预期结果、实际结果栏、证据采集方式、失败回退方式、风险提示和负责人。
- 验证项目应按依赖和风险排序，尽量复用一次接线、一次软件安装和一次工程导入，减少电气工程师现场时间。
- 对验证中发现的问题，应形成可复现记录并回到自动化测试中，避免相同问题再次依赖人工发现。

## 产品安全边界

- 当前阶段不得实现或暴露真实 PLC 下载、RUN/STOP、强制输出、安全 PLC 自动生成或绕过安全回路的接口。
- 任何未来写入 PLC 或控制设备的能力都必须经过显式授权、目标确认、权限隔离、审计记录、失败回退和硬件实测。
- 模型只能提供解释、结构化补全或受限补丁；确定性校验、版本操作、锁定门禁和安全判断不得依赖模型决定。
- 原始 Excel、锁定快照、生成工件和 Git 历史保持不可变或可追溯；恢复操作不得改写历史。

## 交付要求

- 每个开发批次必须包含实现、自动化测试、状态文档、已知限制和验证等级。
- 在黄金项目资料到位前，只能标记“代码完成/自动验证完成，黄金项目验收待进行”，不得标记最终验收通过。
- 在 GX Works3、GX Simulator3、MX Component 和真实 PLC 未实际验证前，相关页面必须显示“尚未接入真实能力”或“未验证”。
- 代码提交应按可独立验收的批次组织；推送远端前运行对应的后端、前端、构建和 E2E 门禁。
